# GOAA 里程碑檢查表 — 2026-06-13

> F-lite 第三刀:approval 持久化閉環(C + A + B 三刀)。換窗口接手 → 乾淨收尾。
> 以三端 git HEAD + repo 為最終事實。三端對齊:DO = GitHub = `0930806`。

## 今日完成(F-lite 第三刀:approval 持久化閉環)

### 三刀 commit(本窗口淨產出)

| 刀 | Commit | 動檔 | 變更 | 驗證 |
|:--|:------|:----|:----|:----|
| **C** approve 寫 PG | `671c32f` | `infra/router/api.py` | +5 行:approve 端點 UPDATE tasks(status/approved_by/approved_at/approval_note) | dispatch→awaiting_approval→approve→PG 四欄真寫入,非 HTTP 200 假成功 |
| **A** rebuild 恢復 | `22906de` | `infra/router/db.py` | +1 詞:`task_rebuild_pool` status IN 加 `awaiting_approval` | 重啟後 `Rebuilt 1 tasks from PG` 真日誌,approve 不再 404 |
| **B** dispatcher 落盤 | `0930806` | `infra/router/api.py` | +4 行:dispatcher 偵測 awaiting_approval 時 `task_upsert(tid, task)` 寫 PG | dispatch 後不手動改 PG,PG 自動變 awaiting_approval;logger 無報錯 |

### 完整數據流(C+A+B 串成閉環)
```
dispatch → PG(pending) + 記憶體池
  → auto_dispatcher 偵測 P0/P1 + risk≥4
    → task_upsert(PG=awaiting_approval)          ← B 刀(偵測即落盤)
    → 記憶體 status=awaiting_approval
  → 重啟 Router
    → rebuild: status IN(...,'awaiting_approval') ← A 刀(重啟恢復)
    → 池恢復 awaiting_approval
  → POST /task/approve
    → UPDATE tasks SET status='approved'...       ← C 刀(端點寫 PG)
    → INSERT audit_log
```

### 工程紀律亮點(本窗口)
- [x] **失敗安全 patch**:三刀皆用內建斷言腳本(錨點唯一 / 變數真存在 / 冪等 / 語法檢查),任一不符 abort 不寫檔;改後先看 `diff` + NormMD5(#12),確認乾淨才重啟
- [x] **變數名零推測**:接手交接包標「未驗證」的 `safe_actor`/`safe_note`/`req.task_id`,經真實代碼(L485-486)驗證後才下刀(#24/#36)
- [x] **#28 守線**:B 刀 PG 寫失敗用 `logger.error` 記錄,**拒絕 `except: pass` 靜默失敗**(過程中攔下一次 pass 版本並回滾)
- [x] **範圍鎖定(#30)**:A 刀只加 `awaiting_approval` 不加 `approved`(approved 無消費點、全載入會脹池);B 刀只補 awaiting_approval,blocked/assigned 無證據需落盤故不碰
- [x] **三端鐵律(#42)**:每刀 commit 前看 staged diff、只 cp 該動的檔、push 後驗 DO = GitHub HEAD

## roadmap 進度推進(對齊 v1.2)
- **P1.2.0**(tasks 表 PG 持久化)🟡 → 推進:今天接入 approve 寫 PG + dispatcher awaiting_approval 落盤 + rebuild 恢復 3 個點;**仍未全完成**(`/tasks/next`、`/task/complete`、tool_invocations hook point 未碰,維持 🟡)
- **F 線**:今天為 F-lite approval 鏈的**持久化實作**,非 F 線設計文檔(`f-line-execution-layer.md` 仍待起草)

## 🔴 待辦 / Backlog
- [ ] **P1↑ DO systemd unit 根治**:8080 埠反覆競爭 + 今日一次孤兒進程佔埠(非 systemd MainPID)。今日靠 `fuser -k :8080` 手動救回。根治:unit 加 `ExecStartPre=fuser -k 8080/tcp` + `Restart=on-failure`。**已實際干擾開發驗證,優先級提高,但須清醒時單獨一刀(改啟動配置,改壞則 Router 起不來)**
- [ ] **F-lite 2b**:reject 端點(複用 approve 模式)
- [ ] **5188 Console 審批 UI**(依賴持久化就緒,現已具備)
- [ ] **P1.2.0 收尾**:`/task/complete`、`/tasks/next` hook point 接入 task_upsert;tool_invocations 雙寫
- [ ] blocked/assigned 是否需落盤 — 目前無證據需要(#30),待真實需求
- [ ] aika-core-01 補 GitHub SSH key(仍落後 `53f599f`,不影響服務)
- [ ] (待確認來源)reply 端點修復 — 本窗口未出現此項,來源待師兄確認後再入 backlog
- [ ] repo 殘留清理(`_tmp_*`/`.bak`)— **先 `git status` + `ls` 確認真垃圾再清;`.bak` 黃金備份(今日三刀回滾/diff 所依賴)不動**(#11)
- [ ] (低優先)logger 名殘留 `OpenClaw.Router`(OpenClaw 已淘汰),無關整理

## 安全提醒(持續)
- secret / sudo 密碼一律**不印、不貼對話**;export 後讀 `os.environ`(#22)
- 黃金基線今日**不升級**(師兄未明示「升級黃金版」,#14 AI 不自行升)

---
*狀態截至本次收工;以三端 git HEAD + repo 為最終事實。*
*Generated by Claude via AiKa — GOAA Project*
