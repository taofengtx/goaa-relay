# AiKa-2 Bootstrap Quick Guide  
  
## Step 1: Verify Connection  
```  
uname -a && free -h && df -h / && echo "AiKa-2 connected"  
```  
  
## Step 2: System Update  
```  
sudo apt-get update -y  
sudo apt-get install -y git curl python3 python3-pip python3-venv docker.io  
```  
